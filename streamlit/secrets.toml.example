# Copy to .streamlit/secrets.toml for local use, or paste in Streamlit Cloud → Settings → Secrets.
# Never commit the real secrets.toml (it is gitignored).
GEMINI_API_KEY = "your-gemini-api-key-here"
